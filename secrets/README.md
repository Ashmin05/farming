This folder holds local secrets that must never be committed — e.g. the
Google Earth Engine service-account JSON key (GEE_KEY_PATH in .env).

The whole `secrets/` folder is gitignored except this file.
