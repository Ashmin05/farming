# Dev/test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.1.1
pytest-asyncio>=0.23.6
aiosqlite>=0.20.0          # In-memory SQLite driver used by the test database fixture
